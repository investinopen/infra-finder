version: "2"

syncs:
  infra-finder-sync:
    src: "."
    sync_excludes:
      - ".git"
      - "log"
      - "tmp/cache"
      - "*.swp"
      - ".docker-sync/daemon.log"
    sync_strategy: "unison"
