<%= render SubnavBarComponent.new(show_back: true, back_to_solutions: true) do |component| %>
  <% component.with_share_bar do %>
    <%= render ShareBarComponent.new(mode: :comparison, comparison: current_comparison) %>
  <% end %>
<% end %>
<div class="l-back-container">
  <div>
    <%= render ComparisonComponent.new(comparison: current_comparison) %>
  </div>
  <%= render BackToTopComponent.new %>
</div>
