attributes_table title: "Options" do
  row :scoss

  row :solution_categories

  SolutionProperty.has_many_associations.without(:maintenance_statuses, :solution_categories, :web_accessibility_applicabilities).each do |option|
    row option
  end

  SolutionProperty.has_one_associations.without(:staffing_full_time, :staffing_volunteer).each do |option|
    row option
  end
end
